import React from 'react';
import style from './App.scss';

const App = () => (
  <div className={style.root}>
    <aside className={style.side}>side</aside>
    <main className={style.main}>main</main>
  </div>
);

export default App;
